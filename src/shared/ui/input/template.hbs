<input
    class="{{#if class}}{{class}} {{/if}}input text{{#if hasError}} text_error-color{{/if}}"
    type="{{type}}"
    name="{{name}}"
    id="{{name}}"
    value="{{value}}"
    placeholder="{{name}}"
    {{#if disabled}}disabled{{/if}}
/>
