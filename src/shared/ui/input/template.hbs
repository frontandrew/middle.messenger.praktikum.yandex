<input
    class="input text{{#if error}} input_error text_error-color{{/if}}"
    type="{{type}}"
    name="{{name}}"
    id="{{name}}"
    value="{{value}}"
    placeholder="{{name}}"
    value="{{value}}"
    {{#if disabled}}disabled{{/if}}
/>
