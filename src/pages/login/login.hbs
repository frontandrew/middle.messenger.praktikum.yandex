{{#> BaseLayout}}
    {{#> FormAuth}}
        <h1 class="auth__title">Authtorisation</h1>
        {{> Field label="Login"}}
        {{> Field label="Password"}}
        {{> Button label="Sign in" type="fill" page="list"}}
        {{> Button label="Sign up" type="link" class="button_red"}}
    {{/FormAuth}}
{{/BaseLayout}}