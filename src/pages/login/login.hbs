{{#> AppContainer}}
    {{#> FormAuth}}
        {{> Field label="Login"}}
        {{> Field label="Password"}}
        {{> Button label="Sign in" type="primary" page="list"}}
        {{> Button label="Sign up" type="link"}}
    {{/FormAuth}}
{{/AppContainer}}